<div class="wrapper">
  <%= render 'bookmarks/navbar' %>
  <div class="description">
    <p>
      <strong>64page</strong> is a service that saves your bookmarks online.
    </p>
    <p>
      If you are new, please <strong>sign up</strong> for user registration. After registering the user, press <strong>New Bookmark</strong> on the top page and register your bookmark
    </p>
    <p>
      <strong>64page</strong>はあなたのブックマークをオンラインで保存するサービスです。
    </p>
    <p>
      初めての方は<strong>sign up</strong>からユーザー登録をお願いします。
      ユーザー登録ができたらトップページで<strong>New Bookmark</strong> を押してあなたのブックマークを登録してください。
    </p>
  </div>
  <div class="login-wrapper">
    <h3>Log in</h3>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field">
        <%= f.label :name %><br/>
        <%= f.text_field :name, autofocus: true, autocomplete: "name", class: 'form-control form-control-sm' %>
      </div>

      <div class="field mt-2">
        <%= f.label :email %><br/>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control form-control-sm' %>
      </div>

      <div class="field mt-2">
        <%= f.label :password %><br/>
        <%= f.password_field :password, autocomplete: "current-password", class: 'form-control form-control-sm' %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="field mt-1">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end %>

      <div class="actions mt-2 mb-2">
        <%= f.submit "Log in", class: 'btn btn-primary p-1 m-2' %>
      </div>
    <% end %>

    <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
      <%= link_to "Sign up", new_registration_path(resource_name), class: 'border_spread_btn mt-2' %>
    <% end %>
  </div>
</div>